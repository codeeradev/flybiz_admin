// User management service — replace with real API calls when backend is ready

export const getUsers = async () => {
  // TODO: Replace with real API call
  // const res = await fetch('/api/users');
  // return res.json();
};

export const updateUserStatus = async (_userId: number, _status: string) => {
  // TODO: Replace with real API call
  // const res = await fetch(`/api/users/${userId}/status`, {
  //   method: 'PATCH',
  //   body: JSON.stringify({ status }),
  // });
  // return res.json();
};

export const deleteUser = async (_userId: number) => {
  // TODO: Replace with real API call
  // await fetch(`/api/users/${userId}`, { method: 'DELETE' });
};
